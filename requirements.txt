# Telegram bot framework (3.29.0 = minimal aiogram allowing fixed aiohttp)
aiogram==3.29.0

# Environment variables
python-dotenv==1.2.2

# HTTP client (async - crypto.py uses shared aiohttp session)
# (requests removed: sync calls blocked the event loop)

# Async support utilities (aiogram ichki ishlatadi, prod barqarorlik uchun)
# Pinned together: aiohttp 3.14.x needs yarl>=1.17 + propcache/frozenlist
aiohttp==3.14.3
yarl==1.24.5
multidict==6.7.1
propcache==0.5.2
frozenlist==1.8.0
aiohappyeyeballs==2.7.1
aiosignal==1.4.0

# PostgreSQL driver (faqat DATABASE_URL bo'lganda ishlatiladi - Supabase/Render)
asyncpg==0.30.0
